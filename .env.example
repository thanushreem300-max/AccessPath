# AccessPath Environment Variables Configuration Example

# FastAPI Backend Configuration
# Port for uvicorn server (default 8000)
PORT=8000
# Enable debug log mode (true/false)
DEBUG=true

# React Frontend Configuration
# API gateway URL proxy target for Vite. 
# Vite dev server will proxy /api calls to this URL.
VITE_API_URL=http://localhost:8000
